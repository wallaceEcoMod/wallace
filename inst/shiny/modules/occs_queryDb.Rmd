```{asis, echo = {{occs_queryDb_knit & !occs_citation_knit}}, eval = {{occs_queryDb_knit & !occs_citation_knit}}, include = {{occs_queryDb_knit & !occs_citation_knit}}}
### Obtain Occurrence Data
You searched the `r "{{occDb}}"` database for *`r "{{spName}}"`*, limited to `r {{occNum}}` records.
```

```{asis, echo = {{occs_queryDb_knit & occs_citation_knit}}, eval = {{occs_queryDb_knit & occs_citation_knit}}, include = {{occs_queryDb_knit & occs_citation_knit}}}
### Obtain Occurrence Data
You searched the `r "{{occDb}}"` database for *`r "{{spName}}"`*, limited to `r {{occNum}}` records, using the citation option (**)
```

```{r, echo = {{occs_queryDb_knit & !occs_citation_knit}}, include = {{occs_queryDb_knit & !occs_citation_knit}}}
# Query selected database for occurrence records
queryDb.{{sp}} <- occs_queryDb(spNames = "{{spName}}", occDb = "{{occDb}}", 
                               occNum = {{occNum}})
occs.{{sp}} <- queryDb.{{sp}}${{sp}}$cleaned
```

```{r, echo = {{occs_queryDb_knit & occs_citation_knit}}, include = {{occs_queryDb_knit & occs_citation_knit}}}
# Query selected database for occurrence records
queryDb.{{sp}} <- occs_queryDb(spNames = "{{spName}}", occDb = "{{occDb}}", 
                               doCitations = {{doCitations}},
                               gbifUser = "{{gbifUser}}", gbifEmail = "{{gbifEmail}}",
                               # Please type your GBIF password (**)
                               gbifPW = "")
occs.{{sp}} <- queryDb.{{sp}}${{sp}}$cleaned
# Print DOI and date of your search (**)
queryDb.{{sp}}${{sp}}$citation
```
